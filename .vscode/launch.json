{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/app/main.go",
            "dlvFlags": ["--check-go-version=false"],
                  "env": {
                "PROFILE": "dev" // ou "prod", "test", etc.
            }
        },
    ]
}